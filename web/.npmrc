# The website depends only on public packages. Pin the public npm registry so
# the lockfile's `resolved` URLs stay portable - a private/authenticated
# registry inherited from a global ~/.npmrc would break `npm ci` in CI and in
# the Docker build (web/Dockerfile).
registry=https://registry.npmjs.org/
