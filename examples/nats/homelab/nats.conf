# Single-node NATS with JetStream — homelab.
# No auth: fine on a trusted LAN bound to a private interface. For anything
# exposed, switch to the JWT path (../nats/server-jwt.conf + bootstrap-creds.sh).
jetstream {
  store_dir: "/data"
}
http: 8222     # monitoring (http://localhost:8222)
