# Single-node NATS with JetStream - homelab, NO-AUTH variant
# (docker-compose.no-auth.yml only). The default stack is authenticated: see
# auth/server.conf + auth/README.md. Use this only for the minimal local demo.
jetstream {
  store_dir: "/data"
}
http: 8222     # monitoring (http://localhost:8222)
