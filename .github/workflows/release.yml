name: Release

# Manually triggered from the Actions tab. In one run it:
#   1. reads the latest vX.Y.Z tag and bumps the part you pick,
#   2. creates and pushes the new tag,
#   3. builds the quik image and publishes it to GHCR.
#
# Tag + build live in one job on purpose: a tag pushed with the default
# GITHUB_TOKEN does not trigger a separate `on: push: tags` workflow, so
# splitting them would need a PAT. This avoids that.
on:
  workflow_dispatch:
    inputs:
      bump:
        description: Which part of the version to increment
        type: choice
        options: [patch, minor, major]
        default: patch

permissions:
  contents: write # create and push tags
  packages: write # push to ghcr.io

jobs:
  release:
    name: tag and publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # need full history so all tags are visible

      - name: Compute next version
        id: version
        env:
          BUMP: ${{ inputs.bump }}
        run: |
          set -euo pipefail
          latest=$(git tag --list 'v*' --sort=-v:refname | head -n1)
          latest=${latest:-v0.0.0}
          ver=${latest#v}
          IFS=. read -r major minor patch <<< "$ver"
          case "$BUMP" in
            major) major=$((major + 1)); minor=0; patch=0 ;;
            minor) minor=$((minor + 1)); patch=0 ;;
            patch) patch=$((patch + 1)) ;;
          esac
          echo "Bumping ${latest} -> v${major}.${minor}.${patch} (${BUMP})"
          {
            echo "tag=v${major}.${minor}.${patch}"
            echo "semver=${major}.${minor}.${patch}"
            echo "minor=${major}.${minor}"
            echo "major=${major}"
          } >> "$GITHUB_OUTPUT"

      - name: Create and push tag
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          set -euo pipefail
          git tag "$TAG"
          git push origin "$TAG"
          echo "### Released \`${TAG}\`" >> "$GITHUB_STEP_SUMMARY"

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # The proxy image: ghcr.io/quiksh/proxy
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=${{ steps.version.outputs.semver }}
            type=raw,value=${{ steps.version.outputs.minor }}
            type=raw,value=${{ steps.version.outputs.major }}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          target: quik-runtime
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=quik
          cache-to: type=gha,mode=max,scope=quik
          platforms: linux/amd64

      # The service-registration sidecar: ghcr.io/quiksh/quik-register. Cut from
      # the same tag so the sidecar and proxy version in lockstep (they share the
      # KV registration contract). repository_owner, not repository - GHCR images
      # are owner-scoped, so this is ghcr.io/quiksh/quik-register, not …/proxy/….
      - id: meta-register
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/quik-register
          tags: |
            type=raw,value=${{ steps.version.outputs.semver }}
            type=raw,value=${{ steps.version.outputs.minor }}
            type=raw,value=${{ steps.version.outputs.major }}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: quik-register/Dockerfile
          push: true
          tags: ${{ steps.meta-register.outputs.tags }}
          labels: ${{ steps.meta-register.outputs.labels }}
          cache-from: type=gha,scope=quik-register
          cache-to: type=gha,mode=max,scope=quik-register
          platforms: linux/amd64
